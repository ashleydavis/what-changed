//
// The whole of this example's "application": one function, so there is something for a test to
// test and something for what-changed to notice a change to.
//
export function greet(name) {
    return `Hello, ${name}!`;
}
